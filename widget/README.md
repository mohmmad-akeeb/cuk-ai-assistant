# widget/

Embeddable chat widget. Single `widget.js`, installed via one `<script>` tag
on the university site. Shadow DOM isolates styles from the host page.

Talks to: `backend/chat-handler` (Cloud Run chat API).
